# Backend API host. Used by src/services/api.js as the single source of truth
# for where requests are sent — change this instead of editing source files.
REACT_APP_API_URL=http://KELANIYASTORESPC:5000
